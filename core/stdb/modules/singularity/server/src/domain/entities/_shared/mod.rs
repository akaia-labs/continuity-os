//! Contains reusable abstractions like DB key types,
//! as well as entities that don't exist on their own,
//! upon which several top-level entities are built.
//! Such entities essentially represent the foundation
//! of the domain entity ontology.

pub mod actor;
pub mod keys;
pub mod message;
